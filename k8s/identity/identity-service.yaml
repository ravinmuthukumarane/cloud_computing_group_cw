apiVersion: v1
kind: Service
metadata:
  name: identity-service
  namespace: app
spec:
  type: ClusterIP
  selector:
    app: identity
  ports:
    - name: http
      port: 5001
      targetPort: 5001
